### Clone the IOWarp Spack Repo
```bash
cd ${HOME}
git clone https://github.com/iowarp/iowarp-install.git -b 0.22.3
spack repo add iowarp-install/iowarp-spack
```

### Install IOWarp
```bash
spack install iowarp
spack load iowarp
``` 